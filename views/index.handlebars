 <img src="img/burger.png" alt="Eat-Da-Burger!"><br>
    <h1>Eat-Da-Burger!</h1><br>
    <form>
        <h3 id="h3">Enter a burger you want to eat and click the submit button.</h3><br>
        <input type="text" id= "bn" name="burger_name"><br><br>
        <button id="button" type="submit">Submit</button><br><br> 
      </form><br>
      
 {{#each burgers}}
          {{#unless devoured}}
<form>
<input type="hidden" name="devoured" value="false">
<h3>{{this.burger_name}}</h3><button id="devour-burger" type="submit">Devour It!</button></form>
{{/unless}}
        {{/each}}
      <br>

       {{#each burgers}}
          {{#if devoured}}
      <div><h3>{{this.burger_name}}</h3></div>
      {{/if}}
        {{/each}}

      